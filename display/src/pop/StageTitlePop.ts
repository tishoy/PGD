/**
 * 关卡标题弹窗
 * create by tishoy
 * 2018.8.10
 */
class StageTitlePop extends egret.Sprite implements PopBase {
    constructor() {
        super();
        this.initView();
    }

    private initView() {

    }

    // 获取面板宽度
    public getWidth(): number {
        return this.width;
    }

    // 获取面板高度
    public getHeight(): number {
        return this.height;
    }

    public play_loop() {

    }
}